NODE_ENV=development
PUBLIC_BRANCH_NAME=TEST
ORIGIN=http://localhost:8080

# There are two URLs for the database because when deploying with docker,
    # the server use an internal URL to connect to the database
DB_URL=https://db-test.canard.cc # This is the URL used by the server to connect to the database
PUBLIC_DB_URL=https://db-test.canard.cc # This is the URL used by the client to connect to the database

IA_SERVER_URL=http://iaserver:8000